# PUK Dashboard - Umgebungsvariablen
# Kopieren: cp .env.example .env
# Dann .env anpassen (SECRET_KEY generieren!)

# =============================================================================
# DEMO-MODUS
# =============================================================================
# 1 = Demo/Pilot (vereinfachte Authentifizierung, Login ohne Passwort)
# 0 = Produktion (SSO-Proxy erforderlich, kein Auto-Login)
ALLOW_DEMO_AUTH=1
DASHBOARD_ALLOW_DEMO_AUTH=1

# =============================================================================
# SICHERHEIT
# =============================================================================
# Secret Key für Token-Signierung.
# Im Demo-Modus: kann leer bleiben (temporärer Key wird generiert).
# In Produktion: MUSS gesetzt werden!
# Generieren: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Debug-Modus (NUR Entwicklung!)
DEBUG=0
DASHBOARD_DEBUG=0

# =============================================================================
# DATENBANK
# =============================================================================
# SQLite (Standard, keine Installation nötig):
# DATABASE_URL=sqlite:///data/app.db   ← Default, muss nicht gesetzt werden

# PostgreSQL (für Produktion / Docker mit --profile postgres):
# DATABASE_URL=postgresql://dashboard_user:PASSWORT@postgres:5432/puk_dashboard
# DB_PASSWORD=SICHERES_PASSWORT_HIER

# =============================================================================
# LOGGING & KONFIGURATION
# =============================================================================
LOG_LEVEL=INFO
TZ=Europe/Zurich
MAX_CSV_ROWS=10000
MAX_CSV_FILE_SIZE_MB=50
SESSION_TIMEOUT_MINUTES=480

# ══════════════════════════════════════════════════════════════════════════════
# SSO / OIDC (Produktions-Vorbereitung)
# Aktiviert direktes JWT-Validierung gegen Keycloak / Azure AD / Authentik
# Standard: SSO_ENABLED=0 → Demo-Login aktiv
# ══════════════════════════════════════════════════════════════════════════════
SSO_ENABLED=0
# SSO_JWKS_URL=https://keycloak.intern/realms/puk/protocol/openid-connect/certs
# SSO_AUDIENCE=puk-dashboard
# SSO_CLAIM=preferred_username
